/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Carousel from './src/Carousel';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Carousel);
